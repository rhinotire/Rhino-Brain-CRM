# PostgreSQL connection string (local Postgres or Supabase)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tirepro?schema=public"

# Random secret used to sign session cookies (generate: openssl rand -base64 32)
SESSION_SECRET="change-me-to-a-long-random-string"
